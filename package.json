{
  "scripts": {
    "build": "vite build",
    "predeploy": "yarn build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "autoprefixer": "^10.x",
    "postcss": "^8.x",
    "tailwindcss": "^3.x",
    "vite": "^5.x",
    "@vitejs/plugin-react": "^4.x",
    "gh-pages": "^6.x"
  }
}
